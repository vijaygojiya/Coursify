import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  contentContainer: {
    paddingHorizontal: 16,
    rowGap: 14,
    paddingBottom: 24,
  },
  columnContainer: { columnGap: 12 },
});
export default styles;
